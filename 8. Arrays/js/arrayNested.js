// ARRAY NESTED

const shoppingCart = ["Tablet", "Monitore", "Laptop", "Desktop", ["Processors", "Mother board", "Memorys",],];
console.log(shoppingCart);

// Accessing the array nested
console.log(shoppingCart[4]); // By accessing index "4" we are accessing the nested array

// Accessing an element from a nested array
console.log(shoppingCart[4][0])